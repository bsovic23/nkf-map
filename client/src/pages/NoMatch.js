import React from 'react';

function NoMatch(){
    return(
        <section class="no-match">
            ERROR 404: Sorry! There is no url that matches!
        </section>
    )
};

export default NoMatch;